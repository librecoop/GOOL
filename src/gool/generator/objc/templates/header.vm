## Velocity template for writing out classes .h in Objective C
#parse($macros)

#includes ($Helper.printDependencies($class))

#if ($class.isEnum())
enum $class.Name {
	#foreach( $field in $class.Fields )
	    $field.Name,
	#end
};
#elseif (!$class.Interfaces.isEmpty())
@interface $class.Name #if(!$class.Interfaces.isEmpty()): #interfaces($class.Interfaces)#end{
	#foreach( $field in $class.Fields )
	    $field;
	#end
}
	#foreach( $method in $class.Methods )
		
		#if($method.isConstructor())-(id)init#foreach( $p in $method.getParams())and$p.Name:($p.Type)$p.Name #end;
	    #elseif(!$method.isMainMethod())($method.Type)$method.Name:#foreach($p in $method.getParams())#if($velocityCount != 1)and$p.Name:#end($p.Type)$p.Name #end;#end
	#end
	
@end
#else
@interface $class.Name #if(!$class.Interfaces.isEmpty()): #interfaces($class.Interfaces)#end{
	#foreach( $field in $class.Fields )
	    $field;
	#end
}
	#foreach( $method in $class.Methods )
		
		#if($method.isConstructor())-(id)init#foreach( $p in $method.getParams())and$p.Name:($p.Type)$p.Name #end;
	    #elseif(!$method.isMainMethod())($method.Type)$method.Name:#foreach($p in $method.getParams())#if($velocityCount != 1)and$p.Name:#end($p.Type)$p.Name #end;#end
	#end
	
@end
#end
